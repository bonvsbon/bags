# API base baked into the production build (the APK).
# Points at THIS computer's LAN IP — the phone must be on the SAME Wi-Fi.
# If your computer's IP changes, update this and rebuild (npm run build && npx cap sync).
VITE_API_URL=http://192.168.4.117:8000/api/v1
